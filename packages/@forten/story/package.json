{
  "name": "@forten/story",
  "version": "0.0.0",
  "description": "Simple component story book.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fortenjs/forten.git"
  },
  "author": "Maia Taewana <maia@fortenjs.org>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p setup/tsconfig.${NODE_ENV:=development}.json",
    "postbuild": "cp package.json ../../node_modules/@forten/story",
    "clean": "rimraf .cache dist coverage ../../node_modules/@forten/story",
    "prepare": "rimraf dist && cp -r ../../node_modules/@forten/story/dist dist",
    "stories": "parcel serve --port 8888 --no-autoinstall src/stories/index.html",
    "test": "jest -c setup/jest.js",
    "test:watch": "jest -c setup/jest.js --watch --coverage false"
  },
  "dependencies": {
    "@forten/build": "next",
    "@forten/theme": "next",
    "jest-styled-components": "^7.0.3",
    "parcel": "^1.12.4"
  },
  "devDependencies": {}
}