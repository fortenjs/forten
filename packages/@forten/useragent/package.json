{
  "name": "@forten/useragent",
  "version": "0.0.0",
  "description": "Handle keyboard useragent.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fortenjs/forten.git"
  },
  "author": "Maia Taewana <maia@fortenjs.org>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p setup/tsconfig.${NODE_ENV:=development}.json",
    "clean": "rimraf .cache dist coverage ../../node_modules/@forten/useragent",
    "postbuild": "cp package.json ../../node_modules/@forten/useragent",
    "prepare": "rimraf dist && cp -r ../../node_modules/@forten/useragent/dist dist",
    "stories": "parcel serve --port 8888 --no-autoinstall src/stories/index.html",
    "test:watch": "jest -c setup/jest.js --watch --coverage false",
    "test": "jest -c setup/jest.js"
  },
  "dependencies": {
    "@forten/build": "next",
    "@forten/hooks": "next",
    "@forten/preferences": "next",
    "bowser": "^2.11.0",
    "workbox-window": "^5.1.4"
  },
  "devDependencies": {
    "@forten/story": "next",
    "@forten/styled": "next"
  }
}